# 饼饼SHOW 网页工具 UI 范式

版本：1.0 · 2026-09-05。基准项目：旋转展台（transparent-3d-turntable）。本文依据当前网页的实际样式和交互整理，包含用户已确认的无描边滑块方案，可作为以后制作图片、视频、模型及其他参数化创作工具的设计输入。

## 1. 一句话定义

浅灰底、白色工作区、黑色标题条，以左侧大预览和右侧紧凑参数栏构成工作台；主体使用直角矩形，黑白反转表示选择状态，黄色仅点缀品牌，滑块采用无描边的等粗双色轨道。内容和操作优先，文字直接描述功能。

这是本网页的独立视觉范式。引用“饼饼SHOW 网页工具 UI 范式”时，以本文的浅色双栏为准；饼饼运镜 C 号在本项目中提供的是模型调光语义，不代表要把网页换成原生软件的深色三栏。

## 2. 页面结构

桌面采用左右双栏：左侧预览占据剩余空间，右侧参数栏宽 320–360px；页面外边距与栏间距均为 18px。两块工作区使用白底、直角、无外描边，整体允许极轻的阴影，用浅灰页面底色区分区域。

左侧顶部是一条 43px 高的黑色标题栏，中文工具名称靠左，简短英文功能说明靠右。主体预览居中摆放，保留留白；本工具的预览框为 4:3，复用时应按新工具的内容比例确定。透明素材使用棋盘格提示透明区域，棋盘格属于界面背景，不能被画进导出文件。恢复视角这类辅助操作贴近预览边角。

右侧顶部放语言入口、品牌、通栏导入按钮和一行素材状态。头部不随参数滚动，常规高度为 165px；参数主体单独纵向滚动，横向不溢出。当前操作顺序是旋转、输出格式、输出尺寸、帧率、灯光与画面、状态和导出，复用时保留“导入 → 参数 → 输出”的顺序，替换中间的业务分组。

导出区处于参数内容末尾，内容较少时利用剩余空间靠下，内容较多时随参数区滚动；它不是固定悬浮、覆盖其他控件的底栏。低频参数可以折叠展开，展开内容留在同一个参数栏内。

## 3. 颜色与边界

| 用途 | 当前值 | 使用原则 |
| --- | --- | --- |
| 页面背景 | `#f5f5f7` | 衬托白色工作区 |
| 工作区、输入框 | `#ffffff` | 清楚、轻量的内容底色 |
| 主文字、选中项、主按钮 | `#1d1d1f` | 主要操作和视觉重心 |
| 顶部标题栏、主按钮悬停 | `#000000` | 白字黑底 |
| 参数标签 | `#424245` | 与主标题形成层级 |
| 次要文字、状态、数值 | `#6e6e73` | 辅助信息 |
| 单位、弱提示 | `#86868b` | 数值旁的弱化文字 |
| 输入框边线、分隔线 | `#d2d2d7` | 1px 细线 |
| 滑块未经过的轨道 | `#e5e5e7` | 实心浅灰，无描边 |
| 品牌下划线 | `#ffe600` | 当前高 6px，仅作品牌点缀 |
| 禁用主按钮 | `#a1a1a6` | 同时使用真正的 disabled 状态 |
| 当前暂停状态按钮 | `#ff3b30` | 局部状态色，不扩展成通用强调色 |

“去掉描边”专指所有滑块轨道和圆形滑钮。数字输入框、未选中的选项按钮、分组细线仍保留自己的 1px 边线，灯光展开容器保留黑色边线。不要把这条要求扩大成整页所有边线都删除。

页面主体、按钮、数字输入框和折叠面板均使用直角。圆形只用于滑块圆钮，轨道端部允许半圆收口，进度条允许圆角。工作区阴影当前为 `0 8px 30px rgba(0,0,0,0.05)`，避免给每个参数或按钮再叠加浮起阴影。

## 4. 字体与密度

字体顺序为 Inter、系统无衬线字体、macOS 系统字体，以及 PingFang SC / Microsoft YaHei 中文回退。Inter 未加载时使用系统字体即可，不应因为缺字体让界面等待或空白。

| 层级 | 字号 | 字重 |
| --- | --- | --- |
| 顶部中文名称 | `clamp(13.6px, 1.12vw, 19.2px)` | 800 |
| 英文功能说明 | `clamp(8.5px, 0.7vw, 12px)` | 650 |
| 品牌文字 | `clamp(17px, 1.4vw, 24px)` | 850 |
| 参数分组标题 | 14px | 780 |
| 参数名称 | 13px | 680 |
| 主按钮 | 导出为 13px | 720 |
| 快捷选项、滑块数值 | 12px | 650 |
| 输入框单位 | 12px | 继承上下文 |

参数栏常规水平内边距为 16px，分组内部间距 9px，标签与控件间距 7px。大分组之间以 1px 细线分隔，上下分别留约 20px；两列输入框之间留 10px，两列选项之间留 8px，四列快捷按钮之间留 6px。灯光展开面板内边距 14px，参数行间距 13px。

## 5. 控件规范

### 滑块：必须统一执行

所有 `input[type="range"]` 共用同一套样式，包括旋转速度、曝光、环境光、主光、补光、阴影、角度和位置。轨道左右两段都高 6px，左段深黑、右段浅灰，两段均没有描边和阴影；轨道端部圆角为 3px。圆钮直径为 16px，实心深黑，无描边、无阴影。输入元素本身高 20px，为轨道与圆钮留出操作空间。

参数名称放上方左侧，当前数值放同一行右侧，滑块放在下一行。需要精确输入的参数可在滑块附近配数字输入框。轨道填充终点必须位于圆钮中心，不能仅按整个输入框宽度计算百分比；当前圆钮半径 8px，其中心实际在左右各内缩 8px 的范围内移动。

归一化进度 `p = clamp((value - min) / (max - min), 0, 1)`，双色分界位置为 `calc(p × 100% + (8 − 16 × p)px)`。这个公式也适用于包含负值的角度和位置滑块。圆钮尺寸改变时必须同步修改半径和轨道分界计算。

关闭浏览器原生 appearance，显式设置 WebKit 的 track/thumb 和 Firefox 的 track/progress/thumb，不能只依赖 accent-color，否则不同浏览器可能重新出现左右粗细不同的原生勾边。WebKit 用两个硬切换的色段绘制同一条轨道，不是渐变过渡效果；Firefox 用同高的轨道和进度层。

拖动、键盘修改、数字输入联动、切换预设、恢复初始值、载入新素材后，都必须同步数值和轨道填充。去掉轨道描边不等于去掉键盘焦点反馈：当前 `:focus-visible` 保留 2px 灰色外轮廓，偏移 3px，只在键盘操作时用于定位控件。

### 数字输入

数字输入使用白底直角框，外层高至少 40px，1px 浅灰边线，数值靠左、单位靠右。单位是独立弱色文字，不混入可编辑数值。输入内容水平内边距约 10px，成对参数如宽度/高度可以并排。输入框与滑块控制同一个值时，要及时同步。

### 选项与预设

少量互斥选项采用平铺按钮，不使用长下拉菜单：两个选项并排，四个选项可以两行两列，尺寸比例或帧率快捷值使用四列。未选中时白底深色字、1px 浅灰边线；选中时深黑底白字。常规选择按钮最低 38px，紧凑预设最低 32px，均为直角。

默认项优先放在左边或第一项。当前色彩映射顺序是 **ACES、Linear**，ACES 默认选中；布光顺序是中性布光、伦勃朗布光。点击预设必须把对应数值真实写入控件；手动调整偏离预设后，不能仍显示该预设处于完全匹配状态。

### 主操作、折叠与状态

导入和导出使用通栏深色按钮，白字，常规高度至少 40px，悬停加深至纯黑。条件未满足时同时呈现灰色和不可执行状态，状态文字放在按钮附近，解释“等待模型”“载入中”“可导出”或实际失败原因。

折叠入口使用直角按钮，收起时白底深字，展开或悬停时黑底白字。展开区域保持清晰的参数纵向顺序，隐藏状态与 `aria-expanded` 同步。图标按钮有可理解的名称或悬停提示，不靠图形本身猜功能。复用时也应为滑块和输入框提供与可见标签对应的可访问名称。

导出进度条高 8px、浅灰底、深色进度，位置紧跟导出按钮。进度必须来自实际工作完成比例。品牌色、选中状态、错误文字和运行状态各自保持明确用途。

## 6. 响应式布局

| 条件 | 当前处理 |
| --- | --- |
| 常规桌面 | 左大预览、右 320–360px 参数栏，外边距与栏间距 18px |
| 宽度至少 761px 且高度不超过 760px | 外边距与栏间距 12px，标题条 30px，参数头部 116px，栏内横向留白 10px；输入框和主按钮约 26px，部分选项约 22px |
| 宽度不超过 760px | 改为上预览、下参数的单栏，页面自然纵向滚动，取消参数区内部滚动 |
| 宽度不超过 560px | 页面外边距 10px，标题允许换行，原两列数字输入改为单列 |

低高度模式压缩间距和控件高度，但仍使用同一套 6px 滑块轨道和 16px 圆钮。以上断点记录当前实现，不代表所有移动设备都已经逐一验收；复用到新项目时仍需实际检查短窗口、窄窗口、长标题和展开面板，保证内容不重叠、导出操作可到达。触屏工具如需扩大操作区域，可扩大外围命中区域，保持轨道视觉比例。

## 7. 文案与交互习惯

名称短而明确，直接使用“导入模型”“旋转速度”“输出尺寸”“灯光与画面”等业务语言。中文为默认，英文或法语按实际需求提供；菜单、单位和状态文字切换后应保持对齐，不应因长文案挤坏布局。品牌与英文说明可以保留，但不要把普通参数替换成不必要的工程术语。

连续参数修改要立即影响预览，默认值和默认选中项一致。状态变化不能只修改按钮颜色而没有实际行为。透明格、拖入提示和恢复视角入口属于编辑界面；导出的是素材本身。原本用户已经确认的动画或灯光效果，不因复用这套 UI 而重新设计。

## 8. 以后直接调用

把本文作为附件，或在能访问该项目文件的任务里提供本文路径，然后使用下面这段指令。仅提范式名称不能保证新任务能取得完整细节。

> 请阅读《饼饼SHOW 网页工具 UI 范式》，按它制作本次工具的界面。采用浅灰页面底色、白色直角工作区、黑色标题条、左侧大预览和右侧紧凑参数栏，使用黑白反转表达选中状态。字体、尺寸、间距和响应式规则参考文档。所有滑块统一为无描边、无阴影、左右等高 6px 的双色轨道，配 16px 实心圆钮；拖动、输入和切换预设都要同步数值与填充。默认选项排在第一位，导入和导出使用通栏深色按钮。把业务内容替换成【本次工具的功能】，保留这套视觉和交互范式。先完成实现与页面验证，再交给我试用。

如果只修改现有网页，可缩短为：“按《饼饼SHOW 网页工具 UI 范式》统一本项目 UI，重点保持白底直角、黑白选择态，以及所有滑块无描边且左右等粗；保持现有功能语义。”

## 9. 复用验收与代码入口

交付时检查：左右栏比例是否正确；标题、品牌、导入和导出层级是否清晰；所有滑块在最小值、中间值、最大值以及负值范围内是否等粗、填充是否跟随圆钮；预设和数字输入联动是否同步；默认项是否处于第一位；折叠、滚动和低高度布局是否能访问所有控件；禁用和键盘焦点是否可识别；透明背景提示是否留在界面中。新项目的响应式或交互验证未做完时，应如实说明。

本项目的样式源文件为 [src/styles.css](src/styles.css)，页面结构及 `updateSliderTracks()`、预设联动位于 [src/main.js](src/main.js)。复制范式时优先复用已实现的样式与同步逻辑，再适配业务。本文记录已确认的 UI 方案及复用要求，不把尚未实现的功能写成现有能力；以后明确修改范式时，同步更新本文和对应控件实现。
